[project]
name = "aegis-core"
version = "0.1.0"
description = "Aegis-Alpha core package"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
