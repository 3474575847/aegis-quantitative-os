[project]
name = "aegis-orchestration"
version = "0.1.0"
description = "Aegis-Alpha research workflow orchestration package"
requires-python = ">=3.12"
dependencies = [
    "aegis-events",
    "aegis-signals",
    "aegis-backtesting",
    "aegis-storage",
    "pydantic",
    "prometheus-client",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
aegis-events = { workspace = true }
aegis-signals = { workspace = true }
aegis-backtesting = { workspace = true }
aegis-storage = { workspace = true }

[tool.pytest.ini_options]
asyncio_mode = "strict"

[dependency-groups]
dev = [
    "pytest-asyncio>=1.3.0",
]
