[project]
name = "aegis-experimentation"
version = "0.1.0"
description = "Aegis-Alpha research experimentation foundation"
requires-python = ">=3.12"
dependencies = [
    "pydantic",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "aiosqlite>=0.20.0",
    "pytest-asyncio>=1.3.0",
    "sqlalchemy>=2.0.0",
]
